pluginManagement {
	repositories {
    maven { url = 'https://repo.spring.io/milestone' }
		gradlePluginPortal()
	}
}

buildCache {
    local {
        directory = "${settingsDir}/build-cache"
    }
} 
rootProject.name = 'server'
